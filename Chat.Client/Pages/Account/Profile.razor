@page "/account/profile"
@using Microsoft.AspNetCore.Authorization
@inherits Razor_Page_Behind_Code_Source.ProfleCodeSource
@attribute [Authorize]
<form>
    <div class="card">
        @* <img src="img.jpg" alt="John" style="width:100%"> *@
        <h1>@UserDto?.FirstName @UserDto?.LastName</h1>
        <p class="title">@UserDto?.Username</p>
        <p>@UserDto?.Bio</p>
        <p>
            <button type="submit" @onclick="GoToUptdaePage">Edit Profile</button>
        </p>
    </div>

   @* <img src="@UserDto?.PhotoUrl"/>
   <h1>@UserDto?.FirstName</h1>
   <h1>@UserDto?.LastName</h1>
   <h1>@UserDto?.Username</h1>
   <h1>@UserDto?.Gender</h1>
   <h1>@UserDto?.Age</h1>
   <h1>@UserDto?.Role</h1>
   <h1>@UserDto?.Bio</h1>
   <h1>@UserDto?.CreatedDate</h1> *@

</form>



<style>
    .card {
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
        max-width: 300px;
        margin: 20px auto;
        text-align: center;
        padding: 16px;
        background-color: #f9f9f9;
        border-radius: 10px;
    }

    .title {
        color: grey;
        font-size: 20px;
        margin-bottom: 10px;
    }

    button {
        border: none;
        outline: 0;
        display: inline-block;
        padding: 12px;
        color: white;
        background-color: #000;
        text-align: center;
        cursor: pointer;
        width: 100%;
        font-size: 18px;
        margin-top: 10px;
        border-radius: 5px;
    }

        button:hover {
            background-color: #444;
        }

    a {
        text-decoration: none;
        font-size: 20px;
        color: black;
        display: block;
        margin-top: 10px;
    }

        a:hover {
            opacity: 0.7;
        }

</style>